## Setup

1. Create a virtual environment and install dependencies:

```bash
python -m venv braille-env
source braille-env/bin/activate  # or .\braille-env\Scripts\activate on Windows
pip install -r requirements.txt
